/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(1);


/***/ },
/* 1 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _array = __webpack_require__(2);

	var _array2 = _interopRequireDefault(_array);

	var _functional = __webpack_require__(4);

	var _functional2 = _interopRequireDefault(_functional);

	var _object = __webpack_require__(3);

	var _object2 = _interopRequireDefault(_object);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var Pareto = {
	    head: _array2.default.head,
	    tail: _array2.default.tail,
	    last: _array2.default.last,
	    flatten: _array2.default.flatten,
	    remove: _array2.default.remove,
	    where: _array2.default.where,
	    curry: _functional2.default.curry,
	    compose: _functional2.default.compose,
	    isEqual: _object2.default.isEqual
	};

	exports.default = Pareto;

/***/ },
/* 2 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

	var _object = __webpack_require__(3);

	var _object2 = _interopRequireDefault(_object);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var isEmpty = function isEmpty(array) {
	    return !(array && array.length > 0);
	};

	var findByMatchingProperties = function findByMatchingProperties(array, properties) {
	    return array.filter(function (entry) {
	        return Object.keys(properties).every(function (key) {
	            return entry[key] === properties[key];
	        });
	    });
	};

	var findIndexByObject = function findIndexByObject(array, element) {
	    if (isEmpty(array) || !element) return -1;

	    for (var i = 0; i < array.length; i++) {
	        if (_object2.default.isEqual(array[i], element)) return i;
	    }
	    return -1;
	};

	var splice = function splice(array, index) {
	    if (isEmpty(array) || !index) return [];

	    var newArray = array.slice(0);
	    newArray.splice(index, 1);
	    return newArray;
	};

	var head = function head(array) {
	    return isEmpty(array) ? undefined : array[0];
	};

	var tail = function tail(array) {
	    return isEmpty(array) ? [] : array.slice(1, array.length);
	};

	var last = function last(array) {
	    return isEmpty(array) ? [] : array[array.length - 1];
	};

	var flatten = function flatten(array) {
	    if (isEmpty(array)) {
	        return [];
	    }
	    return array.reduce(function (a, b) {
	        return a.concat(Array.isArray(b) ? flatten(b) : b);
	    }, []);
	};

	var where = function where(array, props) {
	    if (isEmpty(array) || !props) return -1;

	    if ((typeof props === 'undefined' ? 'undefined' : _typeof(props)) === 'object') {
	        var element = findByMatchingProperties(array, props);
	        return findIndexByObject(array, element[0]);
	    } else {
	        return array.indexOf(props);
	    }
	};

	var remove = function remove(array, props) {
	    if (isEmpty(array)) return [];
	    if (!props) return array;

	    var index = where(array, props);

	    return index > -1 ? splice(array, index) : array;
	};

	var ArrayUtils = {
	    isEmpty: isEmpty,
	    findByMatchingProperties: findByMatchingProperties,
	    findIndexByObject: findIndexByObject,
	    splice: splice,
	    head: head,
	    tail: tail,
	    last: last,
	    flatten: flatten,
	    where: where,
	    remove: remove
	};

	exports.default = ArrayUtils;

/***/ },
/* 3 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var isEqual = function isEqual(a, b) {
	    var aProps = Object.getOwnPropertyNames(a);
	    var bProps = Object.getOwnPropertyNames(b);

	    if (aProps.length != bProps.length) {
	        return false;
	    }

	    for (var i = 0; i < aProps.length; i++) {
	        var propName = aProps[i];

	        if (a[propName] !== b[propName]) {
	            return false;
	        }
	    }

	    return true;
	};

	var ObjectUtils = {
	    isEqual: isEqual
	};

	exports.default = ObjectUtils;

/***/ },
/* 4 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	var curry = function curry(fn) {
		for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
			args[_key - 1] = arguments[_key];
		}

		if (args.length === fn.length) {
			return fn.apply(undefined, args);
		}
		return curry.bind.apply(curry, [undefined, fn].concat(args));
	};

	var compose = function compose() {
		for (var _len2 = arguments.length, fns = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
			fns[_key2] = arguments[_key2];
		}

		return fns.reduce(function (f, g) {
			return function () {
				return f(g.apply(undefined, arguments));
			};
		});
	};

	var FunctionalUtils = {
		compose: compose,
		curry: curry
	};

	exports.default = FunctionalUtils;

/***/ }
/******/ ]);